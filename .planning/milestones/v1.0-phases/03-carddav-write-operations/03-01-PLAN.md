---
phase: 03-carddav-write-operations
plan: 01
type: execute
wave: 1
depends_on:
  - 02-vcard-serialization/02-01
files_modified:
  - src/carddav/mod.rs
  - src/commands/contacts.rs
autonomous: true
requirements:
  - DAV-01
  - DAV-02
  - DAV-03
  - DAV-04

must_haves:
  truths:
    - "CardDavClient can create contacts with PUT and `If-None-Match: *`"
    - "CardDavClient can update contacts with PUT and `If-Match: <etag>`"
    - "CardDavClient can delete contacts with DELETE and `If-Match: <etag>`"
    - "412 responses map to ContactConflict and 404 responses map to ContactNotFound"
    - "Create/update/delete request semantics are covered by offline unit tests"
    - "The codebase has helpers to discover a default addressbook and resolve a contact by ID for phase 4 surfaces"
  artifacts:
    - path: "src/carddav/mod.rs"
      provides: "CardDAV write methods, href construction, write response mapping, lookup helpers"
      contains: "pub async fn create_contact"
    - path: "src/commands/contacts.rs"
      provides: "phase-4-facing contact creation/update merge helpers built on phase 3 contracts"
      contains: "create_contact_record"
  key_links:
    - from: "src/carddav/mod.rs (create_contact)"
      to: "src/carddav/mod.rs (serialize_vcard)"
      via: "CardDAV PUT body uses the phase-2 serializer"
      pattern: "serialize_vcard\\(contact\\)"
    - from: "src/carddav/mod.rs (map_write_response)"
      to: "src/error.rs (ContactConflict / ContactNotFound)"
      via: "HTTP status mapping for write failures"
      pattern: "ContactConflict|ContactNotFound"
---

# Phase 3 Plan 1

Implement the protocol-level CardDAV write layer in `src/carddav/mod.rs`, with offline-verifiable helpers for request semantics and response mapping. Add the small read-side helpers phase 4 will need (`default_addressbook_href`, `get_contact_by_id`) so the higher-level CLI and MCP flows can stay thin.
