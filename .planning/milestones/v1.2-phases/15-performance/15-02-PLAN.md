---
phase: 15-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/jmap/mod.rs
  - src/models/mod.rs
  - src/commands/download.rs
autonomous: true
requirements: [PERF-04, PERF-05, PERF-07, PERF-08]
must_haves:
  truths:
    - "download_blob returns bytes::Bytes without double-allocating through Vec<u8>"
    - "parse_response consumes owned serde_json::Value (no clone of the full subtree)"
    - "Mailbox cache accessors return Arc<Vec<Mailbox>> — cloning the Arc is O(1)"
    - "Session.available_capabilities is stored as Arc<Vec<String>> and cloned as an Arc, not by contents"
  artifacts:
    - path: "src/jmap/mod.rs"
      provides: "Bytes-returning download_blob, owned parse_response, Arc mailbox cache, Arc capabilities"
    - path: "src/commands/download.rs"
      provides: "call sites consuming Bytes via &bytes"
  key_links:
    - from: "src/jmap/mod.rs::download_blob"
      to: "reqwest::Response::bytes()"
      via: "returns bytes::Bytes directly (no .to_vec())"
      pattern: "\\.bytes\\(\\)"
    - from: "src/jmap/mod.rs::parse_response"
      to: "serde_json::Value owned consumption"
      via: "take/destructure, no .clone() on method_responses"
      pattern: "serde_json::Value"
    - from: "src/jmap/mod.rs::cached_mailboxes"
      to: "Arc<Vec<Mailbox>>"
      via: "field type change + Arc::clone on get"
      pattern: "Arc<Vec<Mailbox>>"
---

<objective>
Reduce memory allocations in the JMAP layer: return `bytes::Bytes` from blob downloads, consume owned `serde_json::Value` in parse_response, and wrap the mailbox cache and capabilities list in `Arc` so hot-path calls no longer clone full vectors.

Purpose: Phase 15 goal — memory allocations in the JMAP layer are reduced through Bytes, Arc, and owned-parse patterns (PERF-04, PERF-05, PERF-07, PERF-08).
Output: Refactored JMAP client with Bytes-returning download_blob, owned parse_response, Arc mailbox cache, Arc capabilities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-performance/15-CONTEXT.md
@Cargo.toml
@src/jmap/mod.rs
@src/commands/download.rs

<interfaces>
From src/jmap/mod.rs (current shapes — line numbers approximate):
- Line 35: `available_capabilities: Vec<String>,`
- Line 36: `cached_mailboxes: Option<Vec<Mailbox>>,`
- Line 264: `using: self.available_capabilities.clone(),` (request-building hot path)
- Line 298: `fn parse_response<T: for<'de> Deserialize<'de>>(&self, ..., response: serde_json::Value) -> Result<T>`
- Line 342: `if let Some(ref cached) = self.cached_mailboxes { return Ok(cached.clone()); }`
- Line 366: `self.cached_mailboxes = Some(resp.list.clone());`
- Line 874: `pub async fn download_blob(&self, blob_id: &str) -> Result<Vec<u8>>`

From src/commands/download.rs:
- Line 3: `use crate::util::{extract_text, infer_image_mime, is_image, parse_size, resize_image};`
- Line 54: `let text = extract_text(&bytes, &filename).await?;` (consumes &[u8])

Callers of `download_blob`: `src/commands/download.rs` and `src/mcp/graphql/types.rs` (attachment content resolver — verify with grep).

`bytes::Bytes` is already available transitively via `reqwest`; may need explicit `bytes = "1"` dep in Cargo.toml if used in public signatures.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: download_blob returns bytes::Bytes; call sites use &bytes (PERF-04)</name>
  <files>Cargo.toml, src/jmap/mod.rs, src/commands/download.rs, src/mcp/graphql/types.rs</files>
  <read_first>
    - src/jmap/mod.rs download_blob body (around line 874) — confirm current `.bytes().await?.to_vec()` or equivalent pattern
    - src/commands/download.rs lines 1-80 — identify all `download_blob` call sites and how `bytes` is consumed
    - src/mcp/graphql/types.rs around line 292 (`extract_text(&data, name)`) — confirm data type and usage
    - Cargo.toml — check if `bytes` appears as a direct dep
  </read_first>
  <behavior>
    - Test 1: download_blob signature returns `Result<bytes::Bytes>` (compile-time verified; no runtime test needed if compile passes).
    - Test 2: `bytes::Bytes` can be `.as_ref()`-borrowed to `&[u8]` for existing `extract_text`, `is_image`, `resize_image`, `std::fs::write` call sites.
  </behavior>
  <action>
    Per D-09:

    1. **Cargo.toml**: If `bytes` is not a direct dep, add `bytes = "1"` to `[dependencies]` (alphabetical — between `base64` and `chrono`). Otherwise skip.

    2. **src/jmap/mod.rs::download_blob**:
       - Change return type from `Result<Vec<u8>>` to `Result<bytes::Bytes>`.
       - Inside: replace `response.bytes().await?.to_vec()` (or `.to_vec()` calls) with `response.bytes().await.map_err(...)`. Return the `Bytes` directly — no Vec conversion.
       - Add `use bytes::Bytes;` at the module level if needed.

    3. **src/commands/download.rs**:
       - The local `let bytes = ... .download_blob(...).await?;` binding now holds `Bytes`.
       - Update any callers passing `&bytes` (e.g. `extract_text(&bytes, ...)`): `&bytes` already derefs via `AsRef<[u8]>` — add `.as_ref()` where the compiler needs it. Prefer `bytes.as_ref()` for `&[u8]` contexts and `&bytes[..]` where a slice is explicit.
       - For `std::fs::write(&path, &bytes)` (if present): `fs::write` accepts `impl AsRef<[u8]>`, so `&bytes` or `bytes.as_ref()` is fine — prefer `&bytes`.
       - Do NOT re-introduce `.to_vec()` — that defeats PERF-04.

    4. **src/mcp/graphql/types.rs** (attachment content resolver around line 292):
       - `let data = client.download_blob(...).await?;` → `data: Bytes`.
       - `extract_text(&data, name).await` stays the same (Bytes derefs to &[u8] via AsRef). Adjust to `extract_text(data.as_ref(), name)` if compiler rejects.
       - If `data` is later passed to base64/image functions expecting `&[u8]`, use `data.as_ref()`.

    5. **No new unit test required** — compile success + absence of `.to_vec()` in download_blob body is the contract. Run `cargo build` + existing tests.

    Avoid: changing the function name, wrapping Bytes in Arc (Bytes already refcounts), re-cloning via `.clone()` at call sites (Bytes clone is cheap but unnecessary here — prefer `&bytes`).

    Reference: Phase 15 CONTEXT.md D-09.
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | tail -10 &amp;&amp; cargo test --lib 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    download_blob returns `bytes::Bytes`; no `.to_vec()` in its body; all call sites compile with `&bytes`/`.as_ref()`; existing tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parse_response consumes owned Value + Arc mailbox cache + Arc capabilities (PERF-05, PERF-07, PERF-08)</name>
  <files>src/jmap/mod.rs</files>
  <read_first>
    - src/jmap/mod.rs parse_response signature + body (around line 298) — identify any `.clone()` on the inner subtree
    - src/jmap/mod.rs cached_mailboxes field (line 36), get_mailboxes function (around line 342), cache write (line 366)
    - src/jmap/mod.rs available_capabilities field (line 35), capability population (line 228), `using` request build (line 264)
    - All callers of `parse_response`, `get_mailboxes`, and reads of `available_capabilities` in src/jmap/mod.rs
  </read_first>
  <behavior>
    - Test 1: After 10 calls to `get_mailboxes()` with cache hit, `Arc::strong_count` on the returned Arc grows to at least 10 (proving the Vec is not cloned — only the Arc is).
    - Test 2: parse_response signature takes `serde_json::Value` by value (compile-time).
    - Test 3: `available_capabilities` field type is `Arc<Vec<String>>` (compile-time).
  </behavior>
  <action>
    Per D-10, D-11, D-12:

    1. **PERF-05 — parse_response owned Value (D-10)**:
       - Signature: `fn parse_response<T>(&self, ..., response: serde_json::Value) -> Result<T>` — confirm it already takes by value. If yes, find any `.clone()` inside.
       - Inside: replace `method_responses.clone()` / subtree clones with `Value::take()` on a `&mut Value` OR direct destructuring:
         ```rust
         let serde_json::Value::Object(mut obj) = response else { return Err(...) };
         let method_responses = obj.remove("methodResponses").ok_or(...)?;
         ```
       - Use `serde_json::from_value(owned_value)` (takes ownership) instead of `from_value(value.clone())`.
       - Goal: zero `.clone()` calls on any `serde_json::Value` inside parse_response.

    2. **PERF-07 — Arc mailbox cache (D-11)**:
       - Change field: `cached_mailboxes: Option<std::sync::Arc<Vec<Mailbox>>>,`
       - Change `get_mailboxes` return type from `Result<Vec<Mailbox>>` to `Result<Arc<Vec<Mailbox>>>`.
       - Cache hit: `return Ok(Arc::clone(cached));` (was `cached.clone()` which cloned the Vec).
       - Cache write: `let arc = Arc::new(resp.list); self.cached_mailboxes = Some(Arc::clone(&amp;arc)); Ok(arc)`.
       - Update callers of `get_mailboxes` in this file and any other file (grep first). Callers now receive `Arc<Vec<Mailbox>>`; iterate via `arc.iter()` — no API break visible to end users since Arc derefs to Vec.
       - If callers clone the result for ownership, change to `Arc::clone`.

    3. **PERF-08 — Arc capabilities (D-12)**:
       - Change field: `available_capabilities: std::sync::Arc<Vec<String>>,`
       - Initialization (around line 198-199): `available_capabilities: Arc::new(Vec::new()),`
       - Population (around line 228): build a `Vec<String>`, then `self.available_capabilities = Arc::new(vec);`
       - Request build (around line 264): replace `self.available_capabilities.clone()` with `(*self.available_capabilities).clone()` **only if** the request requires an owned `Vec<String>`. Check the request struct: if the `using` field is `Vec<String>`, we still need an owned clone of the inner Vec (same cost). BUT: if we can change the request serialization to take `&[String]` via `Arc<Vec<String>>` deref OR switch `using` to `Arc<Vec<String>>`, do that to avoid the clone. Simplest correct path: change the request body `using` field to accept `&[String]` and serialize via `&self.available_capabilities[..]`. If that's too invasive, fall back to the explicit `(*arc).clone()` and leave a `// TODO(PERF-08)` comment explaining the remaining clone is at serde serialization boundary only.

    4. **Unit tests** (in `#[cfg(test)]` block at end of src/jmap/mod.rs):
       - `test_mailbox_cache_returns_arc_clone`: construct a JmapClient with a prepopulated Arc cache, call get_mailboxes 10 times, assert `Arc::strong_count(&amp;result) >= 10` (use a test-only helper to seed the cache without HTTP).
       - Skip tests for parse_response and capabilities (compile-time type assertions cover the contracts).

    Avoid: changing public API shape beyond return types, removing the cache entirely, using `Mutex<Arc<_>>` (field is already in `&amp;mut self` context — no Mutex needed).

    Reference: Phase 15 CONTEXT.md D-10, D-11, D-12.
  </action>
  <verify>
    <automated>cargo test --lib jmap::tests::test_mailbox_cache_returns_arc_clone 2>&amp;1 | tail -20 &amp;&amp; cargo build 2>&amp;1 | tail -10 &amp;&amp; cargo clippy --lib --all-features -- -D warnings 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    parse_response has no `.clone()` on Value subtrees; cached_mailboxes field is `Option<Arc<Vec<Mailbox>>>`; get_mailboxes returns Arc and cache hits clone only the Arc; available_capabilities field is `Arc<Vec<String>>`; strong_count test passes; cargo build + clippy clean.
  </done>
</task>

</tasks>

<verification>
- `cargo build` succeeds.
- `cargo test --lib jmap::` passes.
- `cargo clippy --lib --all-features -- -D warnings` clean.
- `rg "\.to_vec\(\)" src/jmap/mod.rs` does NOT match inside download_blob body.
- `rg "Arc&lt;Vec&lt;Mailbox&gt;&gt;" src/jmap/mod.rs` matches at least once.
- `rg "Arc&lt;Vec&lt;String&gt;&gt;" src/jmap/mod.rs` matches at least once.
</verification>

<success_criteria>
1. `download_blob` returns `bytes::Bytes`.
2. `parse_response` does not `.clone()` any `serde_json::Value` subtree.
3. `cached_mailboxes: Option<Arc<Vec<Mailbox>>>`; `get_mailboxes` returns the Arc.
4. `available_capabilities: Arc<Vec<String>>`.
5. Mailbox cache strong_count unit test passes.
6. All existing tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/15-performance/15-02-SUMMARY.md`
</output>
