# Plan 07-01: Calendar & Event CRUD Transport

## Goal

Implement collection and event CRUD over CalDAV using the new discovery and event-model layers.

## Tasks

1. Add `create/update/delete` calendar operations on `CalDavClient`.
2. Add `list/get/create/update/delete` event operations on `CalDavClient`.
3. Add command-layer record helpers shared by CLI and MCP surfaces.
4. Preserve ETag-safe update/delete behavior and explicit not-found/conflict errors.

## Verification

- `cargo test`
- Confirm command-layer helpers compile cleanly over the new CalDAV transport APIs.
