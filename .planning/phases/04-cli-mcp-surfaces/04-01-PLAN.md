---
phase: 04-cli-mcp-surfaces
plan: 01
type: execute
wave: 1
depends_on:
  - 03-carddav-write-operations/03-01
files_modified:
  - src/commands/contacts.rs
  - src/main.rs
  - src/mcp/graphql/types.rs
  - src/mcp/graphql/mutation.rs
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - MCP-01
  - MCP-02
  - MCP-03

must_haves:
  truths:
    - "CLI exposes contacts create / update / delete subcommands"
    - "CLI update preserves unspecified fields from the current contact"
    - "CLI delete exits non-zero unless `--confirm` or `--yes` is supplied"
    - "GraphQL exposes createContact and updateContact mutations that return contact data"
    - "GraphQL deleteContact uses PREVIEW/CONFIRM token gating"
    - "Shared helper logic is used by both CLI and GraphQL to avoid drift"
  artifacts:
    - path: "src/main.rs"
      provides: "contacts create/update/delete command routing"
      contains: "ContactsCommands::Create"
    - path: "src/commands/contacts.rs"
      provides: "shared contact input, patch, and record-level helpers"
      contains: "pub struct ContactInput"
    - path: "src/mcp/graphql/mutation.rs"
      provides: "createContact, updateContact, deleteContact mutations"
      contains: "async fn create_contact"
    - path: "src/mcp/graphql/types.rs"
      provides: "GraphQL result types for contact mutations"
      contains: "ContactMutationResult"
---

# Phase 4 Plan 1

Build the terminal and MCP surfaces on top of phase 3. Keep the merge/update rules and delete confirmation flow shared and explicit so the behavior stays aligned between both entry points.
