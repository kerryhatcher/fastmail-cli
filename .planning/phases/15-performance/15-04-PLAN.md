---
phase: 15-performance
plan: 04
type: execute
wave: 2
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - Cargo.toml
  - src/util.rs
  - src/commands/download.rs
  - src/mcp/graphql/types.rs
  - src/lib.rs
autonomous: true
requirements: [PERF-06, PERF-09, PERF-11]
must_haves:
  truths:
    - "`cargo build --no-default-features` succeeds and produces a binary without kreuzberg/pdfium"
    - "`cargo build` (default features) still includes document text extraction"
    - "Image resize uses FilterType::Triangle"
    - "tokio dependency uses a narrowed feature list, not `full`, and the crate still builds + tests pass"
  artifacts:
    - path: "Cargo.toml"
      provides: "optional extract feature gate + narrowed tokio features"
      contains: "extract"
    - path: "src/util.rs"
      provides: "feature-gated extract_text + Triangle filter"
  key_links:
    - from: "Cargo.toml [features]"
      to: "kreuzberg optional dep"
      via: "default = [\"extract\"], extract = [\"dep:kreuzberg\"]"
      pattern: "dep:kreuzberg"
    - from: "src/util.rs::extract_text"
      to: "#[cfg(feature = \"extract\")] + stub fallback"
      via: "cfg-gated real impl and no-feature stub"
      pattern: "cfg\\(feature = \"extract\"\\)"
---

<objective>
Finalize Phase 15 smaller tweaks: gate `kreuzberg` behind an optional `extract` cargo feature (default on), switch the image resize filter from Lanczos3 to Triangle, and narrow tokio features from `full` to the minimum subset the project actually uses.

Purpose: PERF-06, PERF-09, PERF-11 — binary size reduction via optional feature, CPU cost reduction on attachment previews, compile-time reduction from narrowed tokio features.
Output: Optional `extract` feature, Triangle filter, narrowed tokio features — all verified via `cargo build` and `cargo build --no-default-features`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-performance/15-CONTEXT.md
@Cargo.toml
@src/util.rs
@src/commands/download.rs
@src/mcp/graphql/types.rs

<interfaces>
Current Cargo.toml:
- `kreuzberg = { version = "~4.4", features = ["archives","bundled-pdfium","email","excel","html","language-detection","office","pdf","xml"] }` (hard dep)
- `tokio = { version = "1.49.0", features = ["full"] }`

Current src/util.rs:
- Line 38: `pub async fn extract_text(bytes: &[u8], filename: &str) -> anyhow::Result<Option<String>>`
- Line 244: `let resized = img.resize(new_width, new_height, image::imageops::FilterType::Lanczos3);`

Call sites of extract_text:
- src/commands/download.rs line 3 import + line 54 call
- src/mcp/graphql/types.rs line 292 call

Tokio usage across project: `#[tokio::main]`, `#[tokio::test]`, `tokio::spawn`, `tokio::sync::Mutex`, `tokio::signal::unix::*` (Phase 14), `tokio::select!`, `tokio::time::*`, `tokio::io::*` via reqwest.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate kreuzberg behind optional `extract` feature (PERF-06)</name>
  <files>Cargo.toml, src/util.rs, src/commands/download.rs, src/mcp/graphql/types.rs, src/lib.rs</files>
  <read_first>
    - Cargo.toml — current kreuzberg dep block and any existing `[features]` section
    - src/util.rs full extract_text implementation (line 38+)
    - src/commands/download.rs line 3 import, line 54 call site
    - src/mcp/graphql/types.rs around line 292 — extract_text usage in attachment resolver
    - src/lib.rs (if exists) or src/main.rs — to confirm where the extract_text path is wired
  </read_first>
  <action>
    Per D-07, D-08:

    1. **Cargo.toml changes**:
       - Mark kreuzberg optional: `kreuzberg = { version = "~4.4", optional = true, features = [...all existing features...] }`
       - Add or extend `[features]` section:
         ```toml
         [features]
         default = ["extract"]
         extract = ["dep:kreuzberg"]
         ```
       - If a `[features]` section already exists, merge (do not duplicate).

    2. **src/util.rs::extract_text**:
       - Gate the real impl:
         ```rust
         #[cfg(feature = "extract")]
         pub async fn extract_text(bytes: &amp;[u8], filename: &amp;str) -> anyhow::Result<Option<String>> {
             // existing kreuzberg-based implementation
         }

         #[cfg(not(feature = "extract"))]
         pub async fn extract_text(_bytes: &amp;[u8], _filename: &amp;str) -> anyhow::Result<Option<String>> {
             Ok(Some("document extraction not enabled in this build".to_string()))
         }
         ```
       - Also gate any `use kreuzberg::*` imports at module top with `#[cfg(feature = "extract")]`.

    3. **Call-site adjustments** (src/commands/download.rs, src/mcp/graphql/types.rs):
       - No changes needed if the stub returns `Ok(Some(...))` with the same signature — both call sites handle `Option<String>`.
       - Verify compilation under both `cargo build` and `cargo build --no-default-features`.

    4. **src/lib.rs / src/main.rs**: no `#[cfg(feature = "extract")]` needed at entry points — the feature gates are internal to util.rs.

    Avoid: removing kreuzberg features (keep the full feature list when the dep IS enabled), changing the `extract_text` signature, exposing a Result::Err stub (per CONTEXT.md: "not enabled" is a valid output, not an error).

    Reference: Phase 15 CONTEXT.md D-07, D-08.
  </action>
  <verify>
    <automated>cargo build --no-default-features 2>&amp;1 | tail -15 &amp;&amp; cargo build 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    `cargo build --no-default-features` succeeds without compiling kreuzberg; `cargo build` (default) still includes kreuzberg and produces a working extract_text; stub returns `Ok(Some("document extraction not enabled in this build".to_string()))`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Triangle filter + narrow tokio features (PERF-09, PERF-11)</name>
  <files>Cargo.toml, src/util.rs</files>
  <read_first>
    - src/util.rs line 244 (Lanczos3 → Triangle)
    - Cargo.toml tokio line (46)
    - Grep full project for `tokio::` usage to validate narrowed feature set covers all call sites
  </read_first>
  <action>
    Per D-14, D-15:

    1. **PERF-09 (D-14)** — src/util.rs line 244:
       - Change `FilterType::Lanczos3` → `FilterType::Triangle`.
       - One-line change.

    2. **PERF-11 (D-15)** — Cargo.toml tokio line:
       - Change `tokio = { version = "1.49.0", features = ["full"] }` to:
         ```toml
         tokio = { version = "1.49.0", features = ["rt-multi-thread", "macros", "io-util", "sync", "time", "signal", "net"] }
         ```
       - Then run `cargo build` + `cargo build --tests` + `cargo test --lib`.
       - **If build fails** due to a missing tokio sub-feature (e.g. `process`, `fs`, `rt`):
         - Identify the missing feature from the error message.
         - Add the minimal required feature to the list.
         - Re-run build. Iterate until green.
         - Document the final feature list in a comment above the tokio line: `# PERF-11: narrowed from "full"; see Phase 15-04 SUMMARY for verification`.

    3. **Verify tokio narrowing** is still functional:
       - `cargo test --lib` green.
       - `cargo build --release` succeeds.
       - Manual grep `rg "tokio::(fs|process|net|io)::" src/` to confirm no missing sub-module usages.

    Avoid: reverting to `features = ["full"]` without documenting the specific missing feature; dropping features listed in CONTEXT.md D-15 without testing.

    Reference: Phase 15 CONTEXT.md D-14, D-15. Phase 14 added `tokio::signal::unix` usage (STAB-04) — `signal` feature is REQUIRED.
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | tail -10 &amp;&amp; cargo build --release 2>&amp;1 | tail -10 &amp;&amp; cargo test --lib 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    Image resize uses `FilterType::Triangle`; tokio features narrowed from `full` to verified minimum subset; cargo build, cargo build --release, and cargo test --lib all succeed.
  </done>
</task>

</tasks>

<verification>
- `cargo build` (default features) succeeds.
- `cargo build --no-default-features` succeeds.
- `cargo build --release` succeeds.
- `cargo test --lib` passes.
- `cargo clippy --all-targets --all-features -- -D warnings` clean.
- `rg "Lanczos3" src/util.rs` finds zero matches.
- `rg 'features = \["full"\]' Cargo.toml` (tokio line) finds zero matches.
- `rg 'dep:kreuzberg' Cargo.toml` finds 1 match.
</verification>

<success_criteria>
1. `extract` cargo feature gates kreuzberg; default-on; `--no-default-features` builds without kreuzberg.
2. extract_text stub returns `Ok(Some("document extraction not enabled in this build".to_string()))`.
3. Image resize uses `FilterType::Triangle`.
4. tokio features narrowed from `full` to an explicit minimum subset; all builds + tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/15-performance/15-04-SUMMARY.md`
</output>
