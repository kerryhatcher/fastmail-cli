---
phase: 14-mcp-layer-refactor
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/mcp/graphql/types.rs
  - src/mcp/graphql/query.rs
  - src/mcp/graphql/mutation.rs
  - src/mcp/graphql/mod.rs
autonomous: true
requirements: [PERF-03, SEC-05, STAB-07]
must_haves:
  truths:
    - "All resolvers read AppContext via ctx.data::<AppContext>() — no JmapContext references remain"
    - "query.rs search_contacts/calendars call ctx.data::<AppContext>()?.get_carddav/get_caldav instead of ::new() per call"
    - "All confirmation_token call sites use AppContext::confirmation_token(&ctx, ...) (HMAC-backed)"
    - "The free function confirmation_token() in types.rs is removed"
    - "clippy::await_holding_lock is denied at module level for src/mcp/"
    - "require_jmap_client helpers in types.rs/query.rs/mutation.rs are removed (consolidated on AppContext)"
  artifacts:
    - path: "src/mcp/graphql/query.rs"
      provides: "search_contacts + calendars use shared DAV clients"
      contains: "get_carddav"
    - path: "src/mcp/graphql/mutation.rs"
      provides: "HMAC-backed token generation at all preview gates"
      contains: "app_ctx.confirmation_token"
    - path: "src/mcp/graphql/types.rs"
      provides: "free confirmation_token removed; require_jmap_client removed"
  key_links:
    - from: "query.rs search_contacts"
      to: "AppContext::get_carddav"
      via: "ctx.data::<AppContext>"
      pattern: "get_carddav"
    - from: "mutation.rs preview gates"
      to: "AppContext::confirmation_token"
      via: "app_ctx.confirmation_token"
      pattern: "\\.confirmation_token\\(&\\["
    - from: "src/mcp/"
      to: "clippy lint"
      via: "#![deny(clippy::await_holding_lock)]"
      pattern: "deny\\(clippy::await_holding_lock\\)"
---

<objective>
Migrate all MCP GraphQL resolvers (query.rs, mutation.rs, types.rs) from the temporary JmapContext shim to AppContext: consolidate require_jmap_client into `AppContext::require_jmap`, replace per-request `CardDavClient::new`/`CalDavClient::new` with `AppContext::get_carddav`/`get_caldav`, replace the `confirmation_token()` free function with `AppContext::confirmation_token`, and audit/scope mutex guards so they drop before downstream I/O where feasible.

Purpose: Delivers PERF-03 (no TLS handshake per call), completes SEC-05 rollout (HMAC tokens everywhere), and addresses STAB-07 (guard-scoping audit + clippy deny). Removes the JmapContext shim introduced in plan 01.
Output: All resolvers read AppContext; DAV clients shared across requests; confirmation tokens HMAC-bound; clippy::await_holding_lock denied module-wide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-mcp-layer-refactor/14-CONTEXT.md
@.planning/phases/14-mcp-layer-refactor/14-RESEARCH.md
@.planning/phases/14-mcp-layer-refactor/14-01-PLAN.md
@src/mcp/graphql/mod.rs
@src/mcp/graphql/types.rs
@src/mcp/graphql/query.rs
@src/mcp/graphql/mutation.rs
@src/mcp/mod.rs

<interfaces>
<!-- Types produced by plan 14-01 and used here. -->

From src/mcp/graphql/mod.rs (created by 14-01):
```rust
pub struct AppContext {
    pub jmap: Option<Arc<Mutex<JmapClient>>>,
    pub carddav: Arc<OnceCell<Arc<CardDavClient>>>,
    pub caldav: Arc<OnceCell<Arc<CalDavClient>>>,
    pub hmac_key: Arc<[u8; 32]>,
}

impl AppContext {
    pub fn new(jmap: Option<Arc<Mutex<JmapClient>>>) -> Self;
    pub fn new_with_key(jmap: Option<Arc<Mutex<JmapClient>>>, key: [u8; 32]) -> Self;
    pub fn confirmation_token(&self, parts: &[&str]) -> String;
    pub fn require_jmap(&self) -> async_graphql::Result<Arc<Mutex<JmapClient>>>;
    pub async fn get_carddav(&self) -> async_graphql::Result<Arc<CardDavClient>>;
    pub async fn get_caldav(&self) -> async_graphql::Result<Arc<CalDavClient>>;
}
```

Current duplicated helpers (to be DELETED):
- src/mcp/graphql/types.rs:11-20 — require_jmap_client
- src/mcp/graphql/query.rs:9-18 — require_jmap_client
- src/mcp/graphql/mutation.rs:13-22 — require_jmap_client
- src/mcp/graphql/types.rs:728 — pub fn confirmation_token (DefaultHasher-based)

Current DAV per-call constructions (to be REPLACED):
- src/mcp/graphql/query.rs:212 — CardDavClient::new (in search_contacts)
- src/mcp/graphql/query.rs:230 — CalDavClient::new (in calendars)

Current confirmation_token call sites in mutation.rs to migrate:
- line 114 (deleteContact), 223 (deleteCalendar-like), 393 (deleteEvent), 459 (sendEmail), 537 (reply), 650 (forward-like)
- types.rs line 266 also has require_jmap_client usage
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate query.rs + types.rs to AppContext (DAV sharing + require_jmap consolidation + guard scoping)</name>
  <files>src/mcp/graphql/query.rs, src/mcp/graphql/types.rs, src/mcp/graphql/mod.rs</files>
  <read_first>
    - src/mcp/graphql/query.rs (all 9 require_jmap_client callsites + lines 200-233 DAV construction)
    - src/mcp/graphql/types.rs:11-20 (require_jmap_client) and line 266 (usage)
    - src/mcp/graphql/mod.rs (AppContext from plan 01)
  </read_first>
  <behavior>
    - Test 1: `ctx.data::<AppContext>()` resolves successfully in a resolver context (smoke test via schema.execute)
    - Test 2: Two consecutive calls to a hypothetical CardDAV query reuse the SAME Arc<CardDavClient> (OnceCell behavior — verify via a test-only method that returns the Arc pointer address, OR verify by counting side-effects)
    - Test 3: JMAP mutex guard is scoped (guard dropped before resolver returns) — grep-verified, not runtime-verified
  </behavior>
  <action>
    In `src/mcp/graphql/query.rs`:
    1. DELETE the `require_jmap_client` free function at lines 9-18.
    2. Replace ALL call sites `let client = require_jmap_client(ctx)?;` with `let client = ctx.data::<super::AppContext>()?.require_jmap()?;` (there are 9 such lines per grep: 27, 50, 65, 81, 110, 150, 163, 174, 182).
    3. Replace `search_contacts` body (lines ~200-216) to use shared CardDAV:
       ```rust
       async fn search_contacts(
           &self,
           ctx: &Context<'_>,
           #[graphql(desc = "Search query — matches name, email, or organization")] query: String,
       ) -> Result<Vec<GqlContact>> {
           let app_ctx = ctx.data::<super::AppContext>()?;
           let client = app_ctx.get_carddav().await?;
           let contacts = client.search_contacts(&query).await?;
           Ok(contacts.into_iter().map(GqlContact::from).collect())
       }
       ```
       Delete the per-call Config::load / username / password / CardDavClient::new block.
    4. Replace `calendars` body (lines ~218-234) identically using `app_ctx.get_caldav().await?` then `client.list_calendars().await?`.
    5. Note: the existing signatures for `search_contacts` and `calendars` may not currently take `ctx: &Context<'_>` — ADD that parameter. Update signatures to include `ctx: &Context<'_>` as the first arg after `&self`.

    In `src/mcp/graphql/types.rs`:
    6. DELETE the `require_jmap_client` free function at lines 11-20.
    7. At line 266 (and any other internal caller inside types.rs impls), replace with `ctx.data::<super::AppContext>()?.require_jmap()?`.
    8. DELETE the `pub fn confirmation_token` free function at line 728 (becomes AppContext method — already added in plan 01). Remove any remaining `use std::hash::Hash` / `DefaultHasher` imports.

    In `src/mcp/graphql/mod.rs`:
    9. DELETE the temporary `JmapContext` shim struct and the `.data(JmapContext { client: ctx.jmap.clone() })` line from build_schema. build_schema should now ONLY call `.data(ctx)` with the AppContext.
    10. Add `#![deny(clippy::await_holding_lock)]` at the top of `src/mcp/mod.rs` (NOT `src/mcp/graphql/mod.rs` — top of the module tree so it applies to all children).

    **Guard scoping for STAB-07 (per RESEARCH.md Pattern 6 clarification):** Because the codebase uses `tokio::sync::Mutex` exclusively, the clippy lint will not fire, and holding a tokio MutexGuard across `.await` is SAFE. The real STAB-07 win comes from:
    - (a) Moving DAV clients out of any lock (done above via OnceCell)
    - (b) Scoping JMAP guards to drop before unrelated I/O where feasible

    For query.rs, where each resolver acquires the JMAP mutex then immediately calls ONE JMAP method and returns, the existing pattern is fine — document this with a comment above the first acquired guard in query.rs: `// Note: tokio::sync::MutexGuard is Send and safe to hold across .await (STAB-07 audit 2026-04-04).`

    Ensure `cargo build --lib` and `cargo clippy --lib --all-targets -- -D warnings` both pass.
  </action>
  <verify>
    <automated>cargo build --lib 2>&amp;1 | tail -30 &amp;&amp; cargo clippy --lib --all-targets -- -D warnings 2>&amp;1 | tail -30 &amp;&amp; cargo test --lib mcp -- --nocapture 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'JmapContext' src/mcp/` returns ZERO matches
    - `grep -rn 'fn require_jmap_client' src/mcp/` returns ZERO matches (duplicates removed)
    - `grep -n 'pub fn confirmation_token' src/mcp/graphql/types.rs` returns ZERO matches
    - `grep -n 'DefaultHasher' src/mcp/graphql/types.rs` returns ZERO matches
    - `grep -n 'CardDavClient::new' src/mcp/graphql/query.rs` returns ZERO matches
    - `grep -n 'CalDavClient::new' src/mcp/graphql/query.rs` returns ZERO matches
    - `grep -n 'get_carddav' src/mcp/graphql/query.rs` matches at least once
    - `grep -n 'get_caldav' src/mcp/graphql/query.rs` matches at least once
    - `grep -n 'ctx.data::<super::AppContext>' src/mcp/graphql/query.rs` matches 9+ times
    - `grep -n 'deny(clippy::await_holding_lock)' src/mcp/mod.rs` matches
    - `grep -n 'STAB-07 audit' src/mcp/graphql/query.rs` matches (documentation comment)
    - `cargo build --lib` exits 0
    - `cargo clippy --lib --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>query.rs + types.rs fully migrated to AppContext; DAV clients shared via OnceCell; JmapContext shim removed; clippy deny in place; STAB-07 tokio::sync::Mutex analysis documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate mutation.rs confirmation_token call sites to AppContext HMAC</name>
  <files>src/mcp/graphql/mutation.rs</files>
  <read_first>
    - src/mcp/graphql/mutation.rs (all confirmation_token callsites: 114, 223, 393, 459, 537, 650)
    - src/mcp/graphql/mod.rs (AppContext::confirmation_token from plan 01)
  </read_first>
  <behavior>
    - Test 1: PREVIEW returns a token; CONFIRM with that token succeeds (no behavior regression)
    - Test 2: CONFIRM with a stale token generated by a DIFFERENT AppContext instance fails with "Missing or invalid confirmation_token"
    - Test 3: All 6 existing confirmation gates still compile and their preview/confirm logic is intact
  </behavior>
  <action>
    In `src/mcp/graphql/mutation.rs`:
    1. DELETE the `require_jmap_client` free function at lines 13-22.
    2. Replace ALL `let client = require_jmap_client(ctx)?;` with `let app_ctx = ctx.data::<super::AppContext>()?; let client = app_ctx.require_jmap()?;` (or split into two lines where `app_ctx` is already needed).

    3. For each `super::types::confirmation_token(&[...])` call site, replace with `app_ctx.confirmation_token(&[...])`:
       - Line ~114 (deleteContact): `let token = app_ctx.confirmation_token(&[&id]);`
       - Line ~223 (delete-masked-email or similar): same pattern
       - Line ~393 (deleteEvent): `let token = app_ctx.confirmation_token(&[&id, calendar_id.as_deref().unwrap_or("")]);`
       - Line ~459 (sendEmail): `let token = app_ctx.confirmation_token(&[&to, &subject, &body]);`
       - Line ~537 (reply): `let token = app_ctx.confirmation_token(&[&email_id, &body]);`
       - Line ~650 (forward): `let token = app_ctx.confirmation_token(&[&email_id, &to, body_str]);`

    4. Ensure `app_ctx` is acquired BEFORE `client = app_ctx.require_jmap()?;` in each resolver so both the token and the client come from the same context reference. Pattern:
       ```rust
       let app_ctx = ctx.data::<super::AppContext>()?;
       let token = app_ctx.confirmation_token(&[&id]);
       // ... preview gate ...
       let client = app_ctx.require_jmap()?;
       let mut client = client.lock().await;
       ```

    5. Remove any now-unused `use super::types::confirmation_token;` import line.

    6. Do NOT touch the `mark_as_spam` resolver — plan 03 handles it.

    7. Add unit tests to mutation.rs `#[cfg(test)] mod tests`:
       - Build two schemas with DIFFERENT hmac keys via `build_schema(AppContext::new_with_key(None, [1u8;32]))` and `build_schema(AppContext::new_with_key(None, [2u8;32]))`.
       - Verify `ctx_a.confirmation_token(&["foo"]) != ctx_b.confirmation_token(&["foo"])`.
       - Verify `ctx_a.confirmation_token(&["foo"]) == AppContext::new_with_key(None, [1u8;32]).confirmation_token(&["foo"])` (stable per key).
       - (These tests can live in mod.rs instead if simpler — co-locate with AppContext.)

    Run full build + clippy + tests.
  </action>
  <verify>
    <automated>cargo build --lib 2>&amp;1 | tail -30 &amp;&amp; cargo clippy --lib --all-targets -- -D warnings 2>&amp;1 | tail -30 &amp;&amp; cargo test --lib mcp -- --nocapture 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fn require_jmap_client' src/mcp/graphql/mutation.rs` returns ZERO matches
    - `grep -n 'super::types::confirmation_token' src/mcp/graphql/mutation.rs` returns ZERO matches
    - `grep -cn 'app_ctx.confirmation_token' src/mcp/graphql/mutation.rs` returns at least 6 matches
    - `grep -n 'ctx.data::<super::AppContext>' src/mcp/graphql/mutation.rs` matches 6+ times
    - `grep -n 'use super::types::confirmation_token' src/mcp/graphql/mutation.rs` returns ZERO matches
    - `grep -n 'mark_as_spam' src/mcp/graphql/mutation.rs` matches (left untouched for plan 03)
    - `cargo build --lib` exits 0
    - `cargo clippy --lib --all-targets -- -D warnings` exits 0
    - `cargo test --lib mcp` exits 0 (new per-instance key test passes)
  </acceptance_criteria>
  <done>All confirmation gates in mutation.rs use AppContext HMAC tokens; per-process key randomness verified in tests; require_jmap_client duplication fully eliminated.</done>
</task>

</tasks>

<verification>
- grep confirms zero JmapContext / require_jmap_client / free-fn confirmation_token references remain in src/mcp/
- cargo build --lib exits 0
- cargo clippy --lib --all-targets -- -D warnings exits 0
- cargo test --lib mcp exits 0 with per-instance HMAC-key isolation test
</verification>

<success_criteria>
- JmapContext shim fully removed; build_schema consumes only AppContext
- All 9 query.rs + 10+ mutation.rs resolvers read ctx.data::<AppContext>()
- search_contacts + calendars use OnceCell-shared DAV clients (zero new CardDavClient/CalDavClient constructions per request)
- All 6 confirmation gates in mutation.rs use HMAC-bound AppContext::confirmation_token
- clippy::await_holding_lock denied at src/mcp/ module root
- STAB-07 tokio::sync::Mutex audit documented inline
</success_criteria>

<output>
After completion, create `.planning/phases/14-mcp-layer-refactor/14-02-SUMMARY.md`
</output>
